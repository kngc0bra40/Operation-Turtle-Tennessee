/* Source-priority adapter for Zillow facts. Zero-valued normalized placeholders are blank, not user decisions. */
(()=>{const api=window.OTZillowFacts;if(!api)return;const map=api.applyToProperty,numericBlank=new Set(['beds','baths','sqft','structureArea','yearBuilt','acres','price','parkingSpaces','assessedValue','annualTaxes']);api.applyToProperty=function(record,parsed,options={}){const prepared=structuredClone(record),sources=prepared.fieldSources||{};for(const fact of parsed?.facts||[]){if(!numericBlank.has(fact.field)||Number(prepared[fact.field])!==0)continue;if(['user-confirmed','county'].includes(sources[fact.field]))continue;delete prepared[fact.field]}return map(prepared,parsed,options)}})();
