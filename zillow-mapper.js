/* Source-priority adapter for Zillow facts. Zero-valued normalized placeholders are blank, not user decisions. */
(()=>{const api=window.OTZillowFacts;if(!api)return;const map=api.applyToProperty,numericBlank=new Set(['beds','baths','sqft','structureArea','yearBuilt','acres','price','parkingSpaces','assessedValue','annualTaxes']),nestedDisplay=['deck','coveredPorch','fencing','otherViews'];api.applyToProperty=function(record,parsed,options={}){const prepared=structuredClone(record),sources=prepared.fieldSources||{},temporary=[];for(const fact of parsed?.facts||[]){if(!numericBlank.has(fact.field)||Number(prepared[fact.field])!==0)continue;if(['user-confirmed','county'].includes(sources[fact.field]))continue;delete prepared[fact.field]}for(const field of nestedDisplay){const value=prepared.propertyIntelligence?.zillowLand?.[field];if(value!==undefined&&prepared[field]===undefined){prepared[field]=value;temporary.push(field)}}const result=map(prepared,parsed,options);temporary.forEach(field=>delete result.property[field]);return result}})();
